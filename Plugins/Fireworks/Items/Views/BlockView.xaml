<mp:ModView x:Class="Fireworks.Items.Views.BlockView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:Fireworks.Items.Views"
        xmlns:vm="clr-namespace:Fireworks.Items.ViewModels"
        xmlns:mp="clr-namespace:McMDK2.Plugin;assembly=McMDK2.Plugin">
    
    <mp:ModView.DataContext>
        <vm:BlockViewViewModel/>
    </mp:ModView.DataContext>
    
    <i:Interaction.Triggers>
    </i:Interaction.Triggers>
    
    <Grid>
        <TextBlock Text="ブロック名" FontSize="15"
                   Margin="10,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <TextBox Text="{Binding BlockName}" FontSize="14"
                 Margin="110,8,10,0" VerticalAlignment="Top"
                 Height="24" />
    </Grid>
</mp:ModView>
